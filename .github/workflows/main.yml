name: CI Pipeline

on:
  push:
    branches: [ "feature-login" ]
  pull_request:
    branches: [ "feature-login" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print Hello
        run: echo "CI Pipeline Running"
